name: Publish website to cpanel
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 2
      - name: FTP-Deploy-Action
        uses: sand4rt/ftp-deployer@v1.4
        with:
          sftp: true
          host: 192.168.1.100
          username: codeitc
          password: Codeit@Cloud123
          port: 22,
          remote_folder: /home/codeitc/public_html/testLaravelGitDeploy/
          connectTimeout: 3600,
