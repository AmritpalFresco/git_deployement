name: Laravel

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: deploye app
      uses: appleboy/ssh-action@master
      with:
        HOST: ${{ secrets.HOST }}
        USERNAME: ${{ secrets.USERNAME }}
        PASSWORD: ${{ secrets.PASSWORD }}
        SCRIPT: 'cd home/codeitc/public_html/testLaravelGitDeploy/  && ./deployer.sh'

